services:
  comfyui:
    build: ./
    image: quinninc/comfy:1.1.0
    container_name: comfyui-worker
    privileged: true
    network_mode: "host"
    env_file: .env
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
