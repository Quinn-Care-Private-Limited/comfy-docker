services:
  comfyui:
    build: ./
    image: quinninc/comfy:1.9.0
    container_name: comfyui-worker
    privileged: true
    runtime: nvidia
    network_mode: "host"
    volumes:
      - /root/gcp.json:/gcp.json
    env_file: .env
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
