###
### Contains regular ubuntu configuration for production
###
services:
  comfyui:
    build: ./
    image: quinninc/comfy:1.1.0
    container_name: comfyui-worker
    network_mode: "host"
    env_file: .env
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
